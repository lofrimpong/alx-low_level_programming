#include "main.h"

/**
 * _isuper - Checks if a letter is super
 * @c: The number to be checked
 * Return: 1 for super letter and 0 for anything else
 */

int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
